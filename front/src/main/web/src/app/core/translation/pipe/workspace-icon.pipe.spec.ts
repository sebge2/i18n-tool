import { WorkspaceIconPipe } from './workspace-icon.pipe';

describe('WorkspaceIconPipe', () => {
  xit('create an instance', () => {
    const pipe = new WorkspaceIconPipe();
    expect(pipe).toBeTruthy(); // TODO issue-125
  });
});
