import { WorkspaceIconCssPipe } from './workspace-icon-css.pipe';

describe('WorkspaceIconCssPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkspaceIconCssPipe();
    expect(pipe).toBeTruthy(); // TODO issue-125
  });
});
