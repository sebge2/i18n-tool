import { HasRepositoryAccessDirective } from './has-repository-access.directive';

describe('HasRepositoryAccessDirective', () => {
  xit('should create an instance', () => {
    // const directive = new HasRepositoryAccessDirective();
    // expect(directive).toBeTruthy();
  });
});
