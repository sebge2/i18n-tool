import { LocalizedPipe } from './localized.pipe';

describe('LocalizedPipe', () => {
  xit('create an instance', () => {
    const pipe = new LocalizedPipe(null);
    expect(pipe).toBeTruthy();
  });
});
