import { TranslationLocaleIconPipe } from './translation-locale-icon.pipe';

describe('TranslationLocaleIconPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslationLocaleIconPipe();
    expect(pipe).toBeTruthy(); // TODO
  });
});
