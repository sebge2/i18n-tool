import { RepositoryIconPipe } from './repository-icon.pipe';

describe('RepositoryIconPipe', () => {
  it('create an instance', () => {
    const pipe = new RepositoryIconPipe();
    expect(pipe).toBeTruthy();
  });
});
