language: java

services:
  - docker

branches:
  only:
    - master

jdk:
  - oraclejdk11

deploy:
  provider: script
  script: "echo '$DOCKER_PASSWORD' | docker login -u '$DOCKER_USERNAME' --password-stdin && mvn clean deploy"
  skip_cleanup: true
