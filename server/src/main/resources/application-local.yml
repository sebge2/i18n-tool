be:
  sgerard:
    i18n:
      base-directory: ${BASE_REPOSITORY}
      security:
        default-admin-password: admin
      scheduled-task:
        cleanup-frequency: '0 2 * * * *'
        cleanup-executions-older-than-days: 31
